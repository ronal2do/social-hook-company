import Immutable from 'seamless-immutable';
import { createReducer } from 'reduxsauce';

// Action Types
export const Types = {};

// Action Creators


// Reducer
const init = Immutable({});

export default createReducer(init, {});
