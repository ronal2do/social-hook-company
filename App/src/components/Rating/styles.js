import { StyleSheet } from 'react-native';
import { metrics } from 'assets/styles';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    marginRight: metrics.smallMeasure
  },
  star: {
    marginRight: metrics.tinyMeasure
  }
});
