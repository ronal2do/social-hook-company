import { StyleSheet } from 'react-native';
import { metrics, colors } from 'assets/styles';

export default StyleSheet.create({
  container: {
    flex: 1,
    padding: metrics.baseMeasure,
    backgroundColor: colors.white
  }
});
